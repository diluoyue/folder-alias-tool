# 文件夹别名工具（Folder Alias Tool）

一个用于给**Windows 文件夹设置显示别名（中文 / 自定义名称）**的桌面工具。  
别名通过`desktop.ini`实现——**不会修改真实文件夹名称或路径**。

> 适合：工具合集、软件仓库、学习资料目录等  
> 实际目录保持英文命名，但资源管理器显示为中文说明，更直观。

---

## ✨ 功能特性

- ✔ **单个设置文件夹别名**
- ✔ **批量扫描目录并一键设置**
- ✔ 支持 **前缀 / 后缀批量生成别名**
- ✔ 支持 **清空别名**
- ✔ 支持 **撤销上次批量操作**
- ✔ 自动写入 `desktop.ini`
- ✔ 自动设置：
  - 文件夹：`System`
  - desktop.ini：`Hidden + System`

👉 设置后只影响「显示名称」，真实文件夹名称保持不变。

---

## 🖥 运行环境

- Windows 10/11
- Python 3.12+
- 无需复杂依赖（Tkinter为系统自带GUI）

> 也可以直接下载**Release中的exe**免 Python运行（见下方 👇）。

---

## 🚀 下载&使用（推荐方式）

👉 前往Releases：

👉 **下载最新的：**
folder_alias_tool.exe
双击即可运行（无需安装）。

---

## 🛠 源码运行方式（可选）

```bash
git clone https://github.com/diluoyue/folder-alias-tool.git
cd folder-alias-tool
py folder_alias_tool.py

打包成独立EXE（开发者）
py -m pip install pyinstaller
py -m PyInstaller -F -w folder_alias_tool.py

生成文件dist/folder_alias_tool.exe

